/* Write a C program to calculate area of a rectangle:
1)usinfhard code inputs
2)using inputs supplied by the uses
*/
#include<stdio.h>

int main(){
    int length=3, breadth=8;
    int area = length*breadth;
    printf("The area of this rectangle is %d", area);
    return 0;
}